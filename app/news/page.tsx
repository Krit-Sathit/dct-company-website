import Link from 'next/link'; import {articles} from '@/lib/data';export default function News(){return <><section className="page-hero"><div className="wrap"><div className="eyebrow">News & activity</div><h1>ข่าวสารและความรู้</h1></div></section><main className="wrap section grid three">{articles.map(a=><Link className="card" href={`/news/${a.slug}`} key={a.slug}><span className="tag">{a.category}</span><h3>{a.title}</h3><p>{a.excerpt}</p><p className="article-meta">{a.date}</p></Link>)}</main></>}
