import {notFound} from 'next/navigation'; import {articles} from '@/lib/data';export default async function Article({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const a=articles.find(x=>x.slug===slug);if(!a)return notFound();return <><section className="page-hero"><div className="wrap"><span className="tag">{a.category}</span><h1>{a.title}</h1><p className="article-meta">{a.date}</p></div></section><article className="wrap section"><p className="lead">{a.excerpt}</p><p>บทความตัวอย่างสำหรับ Staging นี้จัดทำเพื่อแสดงโครงสร้างการเผยแพร่เนื้อหา ผู้ดูแลสามารถแก้ไขเนื้อหา ภาพประกอบ หมวดหมู่ สถานะเผยแพร่ และข้อมูล SEO ผ่าน CMS ได้</p></article></>}
