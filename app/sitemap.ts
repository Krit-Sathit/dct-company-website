import type {MetadataRoute} from 'next';import {products,articles} from '@/lib/data';export default function sitemap():MetadataRoute.Sitemap{const base='https://example.com';return ['','/about','/products','/services','/standards','/news','/faq','/contact','/rfq'].map(x=>({url:base+x,lastModified:new Date()})).concat(products.map(p=>({url:`${base}/products/${p.id}`,lastModified:new Date()})),articles.map(a=>({url:`${base}/news/${a.slug}`,lastModified:new Date()})))}
